package com.lossabinos.serviceapp.models.ui.enums

enum class ConceptCategoryTypeUIModel(val label: String) {
    REPUESTOS("repuestos"),      // repuestos / SPARE_PARTS
    MANO_OBRA("mano de obra"),            // mano_obra / LABOR
    CONSUMIBLES("consumibles"),      // consumibles / CONSUMABLES
    DIAGNOSTICO("diagnostico"),      // diagnostico / DIAGNOSTICS
    TRANSPORTE("transporte"),        // transporte / TRANSPORT
    OTROS("otros")             // otros / OTHER
}